# Check the string is palindrom or not
text = input("Enter the text for checking it is palindrom or not : ")

# Approach 1
# text1 = text[ : : -1]
# if text1 == text:
#     print(f"{text} is a palindrome, because reverse of it is {text1}")
# else:
#     print(f"{text} is not a palindrome, because reverse of it is {text1}")